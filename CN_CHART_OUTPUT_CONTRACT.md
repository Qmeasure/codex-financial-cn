# 中文图表与 ZIP 交付契约

本文件是 `financial-services-cn` 的独立图表、图片和 ZIP 交付契约。任何 skill 生成 PNG、JPG、SVG、图表包或包含图表资源的 ZIP 时，都必须遵守本契约。

## 1. 字体和字号

- 图表唯一指定字体为 `Source Han Serif CN`。
- 只允许 Regular 和 Bold 两种字重。
- 图表标题默认 12pt Bold。
- 坐标轴标题、坐标轴标签和图例默认 9pt Regular。
- 数据标签默认 8pt Regular。
- 图表内部来源默认 7pt Regular，颜色 `#666666`。
- 免责声明或口径注释默认 7pt Regular，颜色 `#666666`。

## 2. 图表结构

- 图表标题、轴标签、图例、数据标签、注释和来源必须中文化。
- 图表内部必须写明来源和数据日期；多来源清单放入随附 Markdown、DOCX、HTML 或 README 的“数据来源与口径说明”。
- 正式图表缺失值使用 `—`，不得输出固定校验标签。
- 柱状图、堆叠柱状图、分组柱状图默认关闭纵坐标横向网格线；保留坐标轴、刻度和必要数据标签。除非用户明确要求或图表类型必须依赖网格线，否则不得显示 major/minor gridlines。

## 3. 导出和 ZIP

- 单张图表导出分辨率默认不低于 1600px 宽或 2x DPI；用于 PPTX/DOCX 嵌入时不得模糊。
- ZIP 包必须包含图表文件、数据来源与口径说明、必要时包含可复算数据表。
- 最终回复必须包含图表或 ZIP 文件路径，并说明未完成的人工审阅事项。
