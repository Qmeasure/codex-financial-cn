---
name: roll-forward
description: 为资产负债表科目构建滚动表：期初余额加本期发生、扣除转回后等于期末余额，且每个组成部分都勾稽到总账。用于月结包和审计支持。
---

## 中文版执行契约

- 默认使用中国大陆金融语境：A股优先，港股和美股兼容；如用户指定市场、币种、会计准则或模板，以用户输入为准。
- 数据来源必须遵守根目录 `DATA_SOURCES_CN.md`：官方披露、用户文件、已授权 MCP/数据库优先；免费源只作辅助；监管、会计、KYC、基金文件和月结判断无依据时写“需确认”。
- 所有产物必须遵守根目录 `CN_OUTPUT_FORMATTING.md`：中文字体栈、中文日期、币种/单位、图表标题、表格表头、来源脚注、风险提示和免责声明都要按中文机构材料处理。
- 用户模板和品牌规范优先，但不得突破中文可读性、来源脚注、币种/单位/日期/口径说明这些底线。
- 保留 DCF、LBO、WACC、EV/EBITDA、IRR、MOIC、NAV、KYC、AML、MCP、CLI 等专业缩写和代码标识。


# 滚动表

给定科目（或科目组）、主体和期间，生成能将期初余额勾稽到期末余额的滚动表。

## 结构

```
期初余额（依据上期结账包）                  X
  + 新增/本期发生                            A
  + 本期已入账预提                           B
  − 前期预提转回                            (C)
  − 付款/结算                               (D)
  ± 重分类/调整                              E
  ± 外币折算                                 F
期末余额（依据期末总账）                    Y
```

## 逐行勾稽

- **期初**：上期结账包，或上期期末日期的总账余额。
- **每个发生额行**：通过 internal-gl MCP 查询总账（科目 + 日期范围 + 分录来源过滤条件）。引用该查询。
- **期末**：期末日期的总账余额。

滚动表**必须加总一致**：`X + A + B − C − D + E + F = Y`。如果不一致，差额就是未解释项目，应明确暴露，不要用 plug 平掉。

## 输出

输出滚动表，包含 “勾稽至” 列，逐行引用总账查询或支持文件；同时输出加总检查（通过/失败，以及如有未解释差额）。
