# 中文 XLSX 交付契约

本文件是 `financial-services-cn` 的中文 Excel / XLSX 交付契约。任何 skill 生成 `.xlsx`、Excel 工作簿、模型、数据包、清单或可导入 Excel 的结构化表格文件时，都必须遵守本契约。用户模板优先，但不得省略中文可读性、来源脚注、币种、单位、日期、口径、检查区和人工审阅边界。

## 1. 工作簿结构

- 工作簿必须包含清晰的中文 sheet 名称。保留 DCF、LBO、WACC、EV/EBITDA、IRR、MOIC、NAV、KPI 等专业缩写。
- 每个模型或数据包必须包含来源/假设区和检查区；检查失败必须用中文说明原因和数据缺口。
- 文件名必须能表达公司、任务、日期和产物类型，例如 `[公司]_DCF模型_[YYYYMMDD].xlsx`。
- 最终回复必须包含 XLSX 文件路径；未生成文件时不得声称文件交付完成。

## 2. 字体、格式和单位

- 中文表头、标签、注释和来源说明使用中文字体栈：`Microsoft YaHei`、`PingFang SC`、`Noto Sans CJK SC`、`Source Han Sans SC`、`SimSun`、`Arial Unicode MS` fallback。
- 表头必须清楚标注指标、期间、币种和单位，例如“收入（人民币百万元）”“EV/EBITDA（倍）”。
- 日期默认使用 `YYYY年M月D日` 或 `YYYY-MM-DD`，同一工作簿内保持一致。
- A股默认人民币，单位优先“万元/亿元”；港股默认港元；美股默认美元。跨市场比较必须标注汇率来源、折算日期和会计准则。

## 3. 公式和颜色约定

- 派生值必须优先使用 Excel 公式，不得把 Python 或聊天中计算出的派生结果硬编码为数字。
- 输入值默认蓝色字体；公式默认黑色字体；外链或跨表链接默认绿色字体。用户模板已有颜色规范时以模板为准，但必须保持一致。
- 关键小计、检查行和错误提示必须可审计，不能只依赖视觉排版。
- 负数默认使用括号；百分比保留 1 位小数；倍数保留 1 位小数并使用“x”或“倍”，同一工作簿内一致。

## 4. 来源和审阅

- 每个关键数据区必须写明来源、数据日期、币种、单位和口径。
- 若工作簿包含图表、表格截图或关键数据块，Exhibit 下方来源标注必须使用很小字体：默认 6pt，最大不得超过 7pt。
- 工作簿 Exhibit 下方只保留最重要的 1 个来源并添加 hyperlink；多来源清单放入来源/假设 sheet，不得在 Exhibit 下方罗列。
- XLSX 单元格、批注、来源行、图表标题和超链接显示文本不得出现固定校验标签；正式表格缺失值使用 `—`。
- 订阅数据源或 MCP 数据必须列出数据源/数据库/MCP 名称和数据日期；没有公开 URL 时不要输出固定缺口标签。
- 官方披露、用户文件或监管材料缺失时，相关判断不得作为正式结论。
- 工作簿必须包含免责声明或审阅说明，说明材料不构成投资、法律、税务、会计或监管建议。

## 5. 结构校验

交付 XLSX 前必须完成以下检查：

- 文件存在且路径可访问。
- 工作簿可打开，核心 sheet 存在。
- 关键公式单元格仍为公式，不是硬编码值。
- 来源/假设区和检查区存在。
- 中文表头、币种、单位、日期和免责声明存在。
- 最终回复包含 XLSX 文件路径和未完成的人工审阅事项。
