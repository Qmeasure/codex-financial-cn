# 中文 PPTX 交付契约

本文件是 `financial-services-cn` 的中文 PowerPoint / PPTX 交付契约。任何 skill 生成 `.pptx`、演示文稿、pitch deck、公司简介页、行业材料或可导入 PowerPoint 的幻灯片文件时，都必须遵守本契约。用户模板和品牌规范优先，但不得省略中文可读性、来源、币种、单位、日期、口径、免责声明和视觉 QA。

## 1. 页面和叙事

- 每页只表达一个核心结论，标题使用中文结论式表达，不使用只有英文名词堆叠的标题。
- 首页或页脚必须标注材料状态、日期、公司/项目名称和必要的免责声明短句。
- 投行、投研、财富管理和基金材料必须使用中文章节名和中文风险提示。
- 文件名必须能表达公司、任务、日期和产物类型，例如 `[公司]_Pitch_Deck_[YYYYMMDD].pptx`。

## 2. 字体和版式

- 唯一指定字体为 `Source Han Serif CN`。PPTX 标题、正文、表格、图表、来源、脚注和免责声明都必须使用该字体。
- 只允许 Regular 和 Bold 两种字重。标题、章节标题、表头和重点标签使用 Bold；正文、表体、来源和免责声明使用 Regular。
- 生成或校验前必须确认当前用户环境能解析 `Source Han Serif CN` Regular/Bold；缺少字体时必须先安装官方 `SubsetOTF/CN` 字体文件，不能静默换用其他字体。
- 标题默认 22pt Bold，行距 1.10。
- 小标题/章节标题默认 16pt Bold，行距 1.15。
- 正文默认 11pt Regular，行距 1.15，段后 2pt。
- 表头默认 8.5pt Bold；表体默认 8.5pt Regular；表格内文本行距 1.15。
- 图表标题默认 11pt Bold；坐标轴、图例和数据标签默认 8pt Regular。
- 图表内部来源默认 7pt Regular，颜色 `#666666`。
- 保留用户模板的母版、品牌色、页边距、logo 和页码规则，但中文正文、表格和图注不得沿用英文默认版式。
- 文字不得溢出形状或遮挡图表；长中文句必须换行、缩小字号或改成更合适的布局。
- 禁止用截图伪造可编辑表格，除非用户明确要求图片化并接受不可编辑风险。

## 3. 表格、图表和来源

- 表格必须清楚标注指标、期间、币种和单位。
- 图表标题、轴标签、图例、数据标签、注释和来源必须中文化。
- 每个图表和关键表格必须标注数据日期和来源；涉及估算、预测或未经审计数据时必须标注“估算”“预测”或“未经审计”。
- 图表来源必须放入图表内部或页面“数据来源与口径说明”区域；图表内部来源固定为 7pt Regular、`#666666`。
- PPTX 页面不得在图表下方堆叠多来源清单。多来源清单放入“数据来源与口径说明”或附录。
- PPTX 正文、表格单元格、脚注、来源、caption 和超链接显示文本不得出现固定校验标签；正式表格缺失值使用 `—`。
- 柱状图、堆叠柱状图、分组柱状图默认关闭纵坐标横向网格线；保留坐标轴、刻度和必要数据标签。除非用户明确要求或图表类型必须依赖网格线，否则不得显示 major/minor gridlines。
- PPTX 中嵌入的 PNG/JPG 图表也必须遵守上述字体、来源和网格线规则；不得因为图表先在 Python、HTML、DOCX 或其他工具中生成而绕过检查。
- 来源链接应使用有意义显示文本；不在页面正文中裸露长 URL。

## 4. PDF 兜底和视觉 QA

交付 PPTX 前必须完成以下检查：

- 文件存在且路径可访问。
- PPTX 可打开或结构校验通过。
- 每页中文标题、正文、图表和脚注可读。
- 无明显文字溢出、遮挡、重叠或错位。
- 图表中文字不乱码、不缺字。
- 柱状图、堆叠柱状图、分组柱状图没有错误显示纵坐标横向网格线，图例、坐标轴标签和来源可读。
- 对外分发或需要视觉一致性时，必须同时导出 PDF 作为字体视觉兜底；无法导出时最终回复必须说明未完成 PDF 兜底。
- 可用时执行 PPTX -> PDF/PNG 渲染或截图 QA；不可用时最终回复必须说明未完成视觉渲染 QA 的原因。
- 最终回复包含 PPTX 文件路径、PDF 路径（如已导出）和未完成的人工审阅事项。
