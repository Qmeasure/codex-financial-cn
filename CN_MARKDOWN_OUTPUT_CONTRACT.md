# 中文 Markdown 交付契约

本文件是 `financial-services-cn` 的中文 Markdown 和聊天正文交付契约。任何 skill 生成 `.md` 文件、研究摘要、备忘录、清单、表格、笔记、邮件草稿或最终聊天摘要时，都必须遵守本契约。纯文本输出可以不落盘，但不得把聊天摘要冒充为文件交付物。

## 1. 标题和结构

- 使用中文标题和清晰层级。正式 Markdown 文件必须以中文一级标题开头。
- 默认结构包含：摘要、关键结论、数据来源与口径说明、正文分析、风险与数据缺口、免责声明。
- 表格必须使用 Markdown 表格，不用空格或制表符伪造列对齐。
- 保留 DCF、LBO、WACC、EV/EBITDA、IRR、MOIC、NAV、KYC、AML、MCP、CLI 等专业缩写和代码标识。

## 2. 来源、口径和链接

- 每个关键结论必须能追溯到用户文件、官方披露、MCP/数据库名称、公告日期、数据日期或页码。
- 链接必须使用 Markdown 链接格式 `[显示文本](URL)`；避免裸 URL。
- 若 Markdown、聊天输出或最终交付说明包含 Exhibit（图表、表格、图片或关键数据块），Exhibit 下方来源标注只保留最重要的 1 个来源，并使用可点击 Markdown 链接；多来源清单放入“数据来源与口径说明”或“参考资料”，不得在 Exhibit 下方罗列。
- 正式 Markdown 导出到 DOCX、PPTX、HTML 或图片时，Exhibit 下方来源标注必须使用很小字体：默认 6pt，最大不得超过 7pt。
- 正式输出的正文、表格、脚注、Exhibit caption 和链接显示文本不得出现固定校验标签；正式表格缺失值使用 `—`。
- 订阅数据源或内部数据库没有公开 URL 时，写明数据源/数据库名称、数据日期；不要输出固定缺口标签。
- 缺少监管、会计、KYC、基金运营、客户适当性或税务依据时，不得作出正式判断。

## 3. 文件和聊天输出边界

- 当 skill 明确承诺 `.md` 文件或用户要求文件时，必须生成真实 Markdown 文件；最终回复必须包含文件路径。
- 当 skill 只要求聊天摘要、表格或即时分析时，可以直接在聊天中输出，但必须包含来源、口径限制和数据缺口。
- 聊天摘要不能替代已承诺的 DOCX、XLSX、PPTX、ZIP 或 Markdown 文件主交付物。
- 未生成已承诺文件时，不得声称任务完成。

## 4. 免责声明

正式 Markdown 文件和面向外部分发的聊天正文必须包含免责声明或审阅边界：

> 本材料由 Codex 金融服务（中国版）辅助生成，仅供具备资质的专业人员审阅和进一步核验，不构成投资、法律、税务、会计或监管建议。任何交易、签批、入账、客户准入或对外分发均需由授权人员独立审阅并批准。

## 5. 最终检查

交付 Markdown 或聊天正文前必须确认：

- 中文标题、表格、来源、币种、单位、日期和口径说明存在。
- 无英文模板残留。
- 无裸 URL。
- 风险、数据缺口和免责声明存在。
- 若生成 `.md` 文件，最终回复包含 Markdown 文件路径。
